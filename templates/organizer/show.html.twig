{% extends 'base.html.twig' %}

{% block title %}Organizer{% endblock %}

{% block body %}
    <h1>Organizer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organizer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organizer.name }}</td>
            </tr>
            <tr>
                <th>Contact_info</th>
                <td>{{ organizer.contactInfo }}</td>
            </tr>
            <tr>
                <th>Website_url</th>
                <td>{{ organizer.websiteUrl }}</td>
            </tr>
            <tr>
                <th>Verified</th>
                <td>{{ organizer.verified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organizer_index') }}">back to list</a>

    <a href="{{ path('app_organizer_edit', {'id': organizer.id}) }}">edit</a>

    {{ include('organizer/_delete_form.html.twig') }}
{% endblock %}
