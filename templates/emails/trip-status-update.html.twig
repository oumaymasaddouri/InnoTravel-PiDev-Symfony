<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Trip Status Update</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: {% if trip.status == 'Approved' %}#28a745{% else %}#dc3545{% endif %};
            color: white;
            padding: 15px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            padding: 20px;
            border: 1px solid #ddd;
            border-top: none;
            border-radius: 0 0 5px 5px;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 12px;
            color: #777;
        }
        .trip-details {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .status {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 3px;
            font-weight: bold;
            color: white;
            background-color: {% if trip.status == 'Approved' %}#28a745{% else %}#dc3545{% endif %};
        }
        .button {
            display: inline-block;
            background-color: #4a6fdc;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Trip Status Update</h1>
    </div>
    <div class="content">
        <p>Hello {{ user.firstName }},</p>
        
        <p>The status of your trip has been updated to <span class="status">{{ trip.status }}</span>.</p>
        
        <div class="trip-details">
            <h3>Trip Details:</h3>
            <p><strong>Departure:</strong> {{ trip.departure|date('Y-m-d H:i') }}</p>
            <p><strong>Arrival:</strong> {{ trip.arrivalDate|date('Y-m-d H:i') }}</p>
            <p><strong>Budget:</strong> {{ trip.budget }}</p>
        </div>
        
        {% if trip.status == 'Approved' %}
            <p>Your trip has been approved! You can now proceed with your travel plans.</p>
        {% else %}
            <p>Unfortunately, your trip has been rejected. Please contact our support team for more information or create a new trip with different parameters.</p>
        {% endif %}
        
        <a href="{{ url('user_view_trip', {'id': trip.id}) }}" class="button">View Trip Details</a>
    </div>
    <div class="footer">
        <p>This is an automated message from InnoTravel. Please do not reply to this email.</p>
    </div>
</body>
</html>
