{% extends 'User/base.html.twig' %}

{% block title %}Confirm Delete{% endblock %}

{% block section8 %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <h3 class="mb-4 text-danger fw-bold">⚠️ Confirm Account Deletion</h3>
                    <p>Are you sure you want to permanently delete your account?</p>
                    <form action="{{ path('account_delete_confirm') }}" method="post">
                        <button type="submit" class="btn btn-danger me-3 px-4">🗑️ Delete</button>
                        <a href="{{ path('user_account') }}" class="btn btn-secondary px-4">❌ Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
